#include <iostream>
using namespace std;


int main() {
    int contador = 0;
    while (contador <= 10)
    {
        cout << "Contador: "<< contador << endl;  
        contador = contador + 2; // Incrementamos contador en 2
    }
    return 0;
}
